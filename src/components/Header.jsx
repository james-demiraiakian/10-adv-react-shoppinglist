import React from 'react';

export default function Header() {
  // const {items} = useItems()
  // const length = items.length
  return (
    <div>
      <h1>Grocery List</h1> # of {/* {length} */}
      {/* {add jsx here for if there is 1 or more items in the list, change plural} */}
      Items
    </div>
  );
}
